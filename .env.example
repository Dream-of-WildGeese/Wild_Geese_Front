# .env는 gitignore 대상이라, 클론 후 이 파일을 .env로 복사해서 사용한다.
VITE_API_BASE_URL=https://ondam-api.duckdns.org

# 회원가입 연동 전까지 쓰는 테스트 계정 id. 비워두면 localStorage 값만 사용한다.
# 8=권민찬(CHILD), 7=류수민(PARENT) — 둘은 같은 가족(familyId=2)이라 편지/가족 화면 확인에 쓸 수 있다.
VITE_DEV_USER_ID=8
